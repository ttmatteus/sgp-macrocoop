// quem renderiza de vdd é o layout.tsx do grupo (app) (tem a trilha entre
// dashboard/historico/perfil/ajustes). so existe pra usePathname() do
// AppShell saber que a rota e essa.
// TODO: ainda so visual - GET /turnos/historico nao ta pronto. troca os
// dados de exemplo do HistoricoScreen (dados de exemplo em mock.ts) pela
// chamada real quando o card da api terminar (ver tipos.ts pro contrato)
export default function HistoricoPage() {
  return null
}
