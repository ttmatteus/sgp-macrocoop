# usada só no server (server actions/proxy), o browser nunca chama a api direto.
# obrigatoria: em dev local aponta pra api local, em prod (vercel) pra url do cloud run
API_URL=http://localhost:3000/api
