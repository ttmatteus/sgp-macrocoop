# deixa vazio em dev, o front descobre a api sozinho pelo host que voce abriu a pagina
# (funciona local e de celular na mesma rede, sem precisar editar ip aqui toda hora)
# em prod (vercel) preenche com a url do cloud run
NEXT_PUBLIC_API_URL=
